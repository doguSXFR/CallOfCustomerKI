{
  "name": "callofcustomerki",
  "version": "0.1.0",
  "private": true,
  "description": "C.O.C.K — AI Phone Agent powered by Twilio + Deepgram + OpenAI + ElevenLabs",
  "workspaces": [
    "shared",
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=backend",
    "dev:frontend": "npm run dev --workspace=frontend",
    "build": "npm run build --workspace=shared && npm run build --workspace=backend && npm run build --workspace=frontend",
    "build:frontend": "npm run build --workspace=frontend",
    "start": "npm run start --workspace=backend",
    "typecheck": "tsc --noEmit -p tsconfig.base.json"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}